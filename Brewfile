# This file is meant to be run with `brew bundle {file}`
# or by running `brew bundle` in the directory where this file resides.
# `brew bundle` just runs the commands in this file, prefixing each with `brew`

install sshfs
# Make sure you now follow the directions from `brew info fuse4x-kext`

install ssh-copy-id